import {
  StyleSheet,
} from 'react-native';

//this is a global stylesheet but it has to be included
const styles = StyleSheet.create({
  container: {
    marginVertical: 16,
    flex: 1,
  }
});

export default styles;